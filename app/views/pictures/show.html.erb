<%= link_to("Back to Pictures", pictures_path) %> | <%= link_to("Edit", edit_picture_path(@picture.id)) %> | <%= link_to("Delete", show_picture_path(@picture.id), method: :delete, data: {confirm: "Are you sure you want to delete this picture?"}) %>

<%= render 'display_picture', picture: @picture %>
